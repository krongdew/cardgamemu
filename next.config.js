// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // การตั้งค่าอื่นๆ ที่คุณมี
}

module.exports = nextConfig